# Production Postgres — copy to Dokploy secrets (or .envs/.production/.postgres).
# NEVER commit real passwords.

POSTGRES_HOST=postgres
POSTGRES_PORT=5432
POSTGRES_DB=kotak
POSTGRES_USER=CHANGE_ME_DB_USER
POSTGRES_PASSWORD=CHANGE_ME_LONG_RANDOM_PASSWORD
